machine:
  node:
    version: 4.2.4
deployment:
  github:
    branch: master
    commands:
      - git config --global user.email "circleci@itsinthenode.com"
      - git config --global user.name "CircleCI Bot"
      - npm run deploy
